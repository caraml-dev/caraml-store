pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/release' }
		mavenCentral()
		gradlePluginPortal()
	}
}
rootProject.name = 'caraml-store'
include('caraml-store-testutil', 'caraml-store-protobuf', 'caraml-store-registry')
